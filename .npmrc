# React Native / Metro can't resolve pnpm's nested/symlinked node_modules layout.
# Expo's official guidance for pnpm monorepos is a hoisted node-linker.
# https://docs.expo.dev/guides/monorepos/#pnpm
node-linker=hoisted
